#define SIZE 100
void initArray(int *array, int SIZE);
void main(void)
{
    int number = 5;
    int numberTwo = 2;
    initArray(&number, SIZE);
}
void initArray(int *array, int size)
{
    int length = (sizeof(array) / sizeof(int));
    for (int i = 0; i < length; i++)
    {
        array[i] = 0;
    }
    array = 5;
}
numebrs = [ 3, 4, 5 ] & numbers[] : ajhdf | gze3bm gseg & number[7] = e
