function getFirstOfHeight(height: number, height_map: number[]) {
    return height_map.indexOf(height)
}
function getLastOfHeight(height: number, height_map: number[]) {
    return height_map.lastIndexOf(height);
}

